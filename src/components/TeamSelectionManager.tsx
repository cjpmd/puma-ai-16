
// Re-export the refactored TeamSelectionManager
import { TeamSelectionManager as RefactoredTeamSelectionManager } from "./team-selection";

// Export the new implementation
export const TeamSelectionManager = RefactoredTeamSelectionManager;
